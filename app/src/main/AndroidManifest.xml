<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tiyas.latihanfragment">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LatihanFragment">
        <activity
            android:name=".CustomView"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ExampleConstraint"
            android:exported="true" />
        <activity
            android:name=".latihanFragment.ActivityProfile"
            android:exported="true" />
        <activity
            android:name=".latihanFragment.MainActivity"
            android:exported="true">

        </activity>
    </application>

</manifest>